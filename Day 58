day 58 : Multiple of 30 plus 1 or 2 program 

In C :
#include <stdio.h>

int main() {
    int num;

    // Ask the user to enter a number
    printf("Enter a number: ");
    scanf("%d", &num);

    // Check if the number is a multiple of 30 plus 1 or 2
    if (num % 30 == 1) {
        printf("%d is a multiple of 30 plus 1.\n", num);
    } 
    else if (num % 30 == 2) {
        printf("%d is a multiple of 30 plus 2.\n", num);
    } 
    else {
        printf("%d is not a multiple of 30 plus 1 or 2.\n", num);
    }

    return 0;
}

O/P :
Enter a number: 61
61 is a multiple of 30 plus 1.

Enter a number: 62
62 is a multiple of 30 plus 2.

Enter a number: 60
60 is not a multiple of 30 plus 1 or 2.

IN PYTHON:
# Ask the user to enter a number
num = int(input("Enter a number: "))

# Check if the number is a multiple of 30 plus 1 or 2
if num % 30 == 1:
    print(f"{num} is a multiple of 30 plus 1.")
elif num % 30 == 2:
    print(f"{num} is a multiple of 30 plus 2.")
else:
    print(f"{num} is not a multiple of 30 plus 1 or 2.")

O/P :
Enter a number: 61
61 is a multiple of 30 plus 1.

Enter a number: 62
62 is a multiple of 30 plus 2.

Enter a number: 60
60 is not a multiple of 30 plus 1 or 2.
