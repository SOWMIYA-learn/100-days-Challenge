day 57 of crew matrix : program that takes N integers as input and then displays them:

IN C :
#include <stdio.h>

int main() {
    int N, i;

    // Ask the user for the number of integers
    printf("Enter the number of integers (N): ");
    scanf("%d", &N);

    int arr[N]; // Declare an array to store N integers

    // Read N integers from the user
    printf("Enter %d integers:\n", N);
    for (i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }

    // Display the N integers
    printf("You entered:\n");
    for (i = 0; i < N; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}

O/P:
Enter the number of integers (N): 5
Enter 5 integers:
10
20
30
40
50
You entered:
10 20 30 40 50

IN PYTHON :
# Ask the user for the number of integers
N = int(input("Enter the number of integers (N): "))

# Read N integers from the user
print(f"Enter {N} integers:")
integers = []
for _ in range(N):
    num = int(input())
    integers.append(num)

# Display the N integers
print("You entered:")
for num in integers:
    print(num, end=' ')

O/P :
Enter the number of integers (N): 5
Enter 5 integers:
10
20
30
40
50
You entered:
10 20 30 40 50
